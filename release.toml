[workspace]
shared-version = true
consolidate-commits = true

publish = false
verify = false
tag-name = "v{{version}}"
pre-release-commit-message = "chore: release v{{version}}"
allow-branch = ["main"]